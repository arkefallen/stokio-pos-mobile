// import 'package:flutter_riverpod/flutter_riverpod.dart';
// import 'package:flutter_test/flutter_test.dart';

// import 'package:stokio_mobile/main.dart';

// void main() {
//   testWidgets('App loads successfully and shows splash screen', (WidgetTester tester) async {
//     // Build our app and trigger a frame
//     await tester.pumpWidget(
//       const ProviderScope(
//         child: StokioApp(),
//       ),
//     );

//     // Verify that our app shows the branding on splash screen
//     expect(find.text('Stokio'), findsOneWidget);
//     expect(find.text(' POS'), findsOneWidget);
    
//     // Wait for animations to settle
//     await tester.pump(const Duration(milliseconds: 500));
//   });
// }
